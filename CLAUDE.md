# CLAUDE.md (loader)

The canonical, tool-neutral project context lives in **[`AGENTS.md`](AGENTS.md)**. Always read it
first when working in this repo — architecture, the `scripts/forge.sh` backend-invocation rule, CLI
conventions, dev commands, and the release flow all live there.

This file is the loader Claude Code auto-reads. Keep it short — project context belongs in `AGENTS.md`,
not here. Add Claude-specific harness notes below only when something genuinely cannot live in the
tool-neutral file.

_No Claude-specific harness notes at the moment._
