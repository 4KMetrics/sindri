# GEMINI.md (loader)

The canonical, tool-neutral project context lives in **[`AGENTS.md`](AGENTS.md)**. Always read it
first when working in this repo — architecture, the `scripts/forge.sh` backend-invocation rule, CLI
conventions, dev commands, and the release flow all live there.

This file is the loader Gemini CLI auto-reads. Keep it short — project context belongs in `AGENTS.md`,
not here. Add Gemini-specific notes below only when something genuinely cannot live in the
tool-neutral file.

_No Gemini-specific notes at the moment._
